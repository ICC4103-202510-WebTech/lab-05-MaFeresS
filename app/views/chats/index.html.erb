<h1>Chats</h1>
<hr>
<table class="table">
  <% @chats.each do |chat| %>
    <tr>
      <td class="bg-dark text-primary">
        <%= link_to("Chat with "+chat.get_other(current_user),chat_path(chat)) %>
      </td>
    </tr>
  <% end %>
</table>
<%= link_to("Start Chat",new_chat_path) %>